import { Targetprocess } from "targetprocess-rest-api";
import { HarvestApi } from "../harvest/api";

export const finish = (harvest: HarvestApi, tp: Targetprocess) => {
    console.log("finishing!");
};
